import { MCPJson } from '../types';
/**
 * Generates an MCP server TypeScript file from the given MCP JSON
 */
export declare function generateMcpServer(mcpJson: MCPJson, outputPath: string, apiBaseUrl: string): void;
